rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // During development, allow all access
      // TODO: Implement proper security rules before production
      allow read, write: if true;
    }
  }
} 